# build output
/apprepo

# релизные сборки (make dist)
/dist/

# local databases and data
*.db
*.db-shm
*.db-wal

# локальный каталог данных из ручных прогонов (scripts/smoke.sh, отладка)
/repo/

# служебные каталоги инструментов (индексы, состояние сессий)
/.omc/
/.repowise/

# macOS
.DS_Store
